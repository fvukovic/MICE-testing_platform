'use strict';

describe('myApp.indeks-info module', function() {

  beforeEach(module('myApp.indeks-info'));

  describe('indeks-info controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var IndeksInfoCtrl = $controller('IndeksInfoCtrl');
      expect(IndeksInfoCtrl).toBeDefined();
    }));

  });
});