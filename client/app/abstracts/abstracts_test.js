'use strict';

describe('myApp.abstracts module', function() {

  beforeEach(module('myApp.abstracts'));

  describe('abstracts controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var abstractsCtrl = $controller('AbstractsCtrl');
      expect(abstractsCtrl).toBeDefined();
    }));

  });
});