'use strict';

describe('myApp.conference-info module', function() {

  beforeEach(module('myApp.conference-info'));

  describe('conference-info controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ConferenceInfoCtrl = $controller('ConferenceInfoCtrl');
      expect(ConferenceInfoCtrl).toBeDefined();
    }));

  });
});